# Local-first RAG assistant

This project is a local-first RAG assistant: ingestion, preprocessing, indexing, retrieval, generation, evaluation, and local delivery should be runnable and debuggable on a developer machine. We will prefer inspectable local artifacts, explicit configuration, and replaceable backend adapters over workflows that require opaque hosted services by default, because retrieval quality and evaluation need to be reproducible during development.
